# Test for a length-1 date-time vector

Test for a length-1 date-time vector

## Usage

``` r
.is_scalar_dttm(x)
```

## Arguments

- x:

  The object to check.

## Value

`TRUE` if `x` is a length-1
[base::POSIXct](https://rdrr.io/r/base/DateTimeClasses.html) vector,
else `FALSE`.
