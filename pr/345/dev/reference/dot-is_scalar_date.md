# Test for a length-1 date vector

Test for a length-1 date vector

## Usage

``` r
.is_scalar_date(x)
```

## Arguments

- x:

  The object to check.

## Value

`TRUE` if `x` is a length-1
[base::Date](https://rdrr.io/r/base/Dates.html) vector, else `FALSE`.
