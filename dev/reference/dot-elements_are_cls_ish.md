# Check if all elements of a list-like object are ish

Check if all elements of a list-like object are ish

## Usage

``` r
.elements_are_cls_ish(x, are_cls_ish_fn, ...)
```

## Arguments

- x:

  The object to check.
