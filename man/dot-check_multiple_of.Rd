% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{.check_multiple_of}
\alias{.check_multiple_of}
\title{Check that all elements are integer multiples of a value}
\usage{
.check_multiple_of(x, multiple_of, x_arg = caller_arg(x), call = caller_env())
}
\arguments{
\item{x}{The object to check.}

\item{multiple_of}{(\code{numeric(1)}, positive) The value \code{x} must be a multiple
of. \code{NULL} skips the check.}

\item{x_arg}{(\code{character(1)}) The name of the object being stabilized to use
in error messages. The automatic value will work in most cases, or pass it
through from higher-level functions to make error messages clearer in
unexported functions.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}
}
\value{
\code{NULL} invisibly (called for side effects).
}
\description{
Doubles are compared with a small relative tolerance
(\code{sqrt(.Machine$double.eps)}, the same default used by
\code{\link[base:all.equal]{base::all.equal()}}) so that representable rounding error (e.g.
\code{0.3 / 0.1}) doesn't produce spurious failures.
}
\keyword{internal}
