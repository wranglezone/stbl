% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabilize_date.R
\name{.to_date_locale_scalar}
\alias{.to_date_locale_scalar}
\title{Coerce to length-1 date, trying \code{accepted_datetime_formats}}
\usage{
.to_date_locale_scalar(
  x,
  ...,
  accepted_datetime_formats,
  allow_null = FALSE,
  allow_zero_length = FALSE,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)
}
\arguments{
\item{x}{The object to stabilize.}

\item{...}{Arguments passed to methods.}

\item{accepted_datetime_formats}{(\code{character}) \code{strptime()}-style format
strings to try, in order, when parsing a character \code{x}. The first format
that parses every non-\code{NA} element of \code{x} is used; if none do, the
result (and any error) is based on the first format tried. Defaults to
\code{\link[=locale_datetime_formats]{locale_datetime_formats()}}, which starts with the unambiguous RFC 3339
shape (\code{"\%Y-\%m-\%d"}, optionally with a time-of-day component) before
falling back to the current locale's conventional date order.}

\item{allow_null}{(\code{logical(1)}) Is NULL an acceptable value?}

\item{allow_zero_length}{(\code{logical(1)}) Are zero-length vectors acceptable?}

\item{x_arg}{(\code{character(1)}) The name of the object being stabilized to use
in error messages. The automatic value will work in most cases, or pass it
through from higher-level functions to make error messages clearer in
unexported functions.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{x_class}{(\code{character(1)}) The class name of the object being stabilized
to use in error messages. Use this if you remove a special class from the
object before checking its coercion, but want the error message to match
the original class.}
}
\value{
The input as a length-1 \link[base:Date]{base::Date} vector.
}
\description{
The scalar counterpart of \code{\link[=.to_date_locale]{.to_date_locale()}}, used by
\code{\link[=stabilize_date_scalar]{stabilize_date_scalar()}} in place of \code{\link[=to_date_scalar]{to_date_scalar()}}.
}
\keyword{internal}
