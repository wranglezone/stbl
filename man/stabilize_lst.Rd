% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabilize_lst.R
\name{stabilize_lst}
\alias{stabilize_lst}
\alias{stabilize_list}
\alias{stabilise_lst}
\alias{stabilise_list}
\title{Ensure a list argument meets expectations}
\usage{
stabilize_lst(
  .x,
  ...,
  .named = NULL,
  .unnamed = NULL,
  .allow_duplicate_names = FALSE,
  .unique = FALSE,
  .allow_null = TRUE,
  .min_size = NULL,
  .max_size = NULL,
  .x_arg = caller_arg(.x),
  .call = caller_env(),
  .x_class = object_type(.x)
)

stabilize_list(
  .x,
  ...,
  .named = NULL,
  .unnamed = NULL,
  .allow_duplicate_names = FALSE,
  .unique = FALSE,
  .allow_null = TRUE,
  .min_size = NULL,
  .max_size = NULL,
  .x_arg = caller_arg(.x),
  .call = caller_env(),
  .x_class = object_type(.x)
)

stabilise_lst(
  .x,
  ...,
  .named = NULL,
  .unnamed = NULL,
  .allow_duplicate_names = FALSE,
  .unique = FALSE,
  .allow_null = TRUE,
  .min_size = NULL,
  .max_size = NULL,
  .x_arg = caller_arg(.x),
  .call = caller_env(),
  .x_class = object_type(.x)
)

stabilise_list(
  .x,
  ...,
  .named = NULL,
  .unnamed = NULL,
  .allow_duplicate_names = FALSE,
  .unique = FALSE,
  .allow_null = TRUE,
  .min_size = NULL,
  .max_size = NULL,
  .x_arg = caller_arg(.x),
  .call = caller_env(),
  .x_class = object_type(.x)
)
}
\arguments{
\item{.x}{The argument to stabilize.}

\item{...}{Named stabilizer functions, such as \verb{stabilize_*} functions
(\code{\link[=stabilize_chr]{stabilize_chr()}}, etc) or functions produced by \verb{specify_*()} functions
(\code{\link[=specify_chr]{specify_chr()}}, etc). Each name corresponds to a required element in
\code{.x}, and the function is used to validate that element.}

\item{.named}{A single stabilizer function, such as a \verb{stabilize_*} function
(\code{\link[=stabilize_chr]{stabilize_chr()}}, etc) or a function produced by a \verb{specify_*()} function
(\code{\link[=specify_chr]{specify_chr()}}, etc). This function is used to validate all named
elements of \code{.x} that are \emph{not} explicitly listed in \code{...}. If \code{NULL}
(default), any extra named elements will cause an error.}

\item{.unnamed}{A single stabilizer function, such as a \verb{stabilize_*}
function (\code{\link[=stabilize_chr]{stabilize_chr()}}, etc) or a function produced by a \verb{specify_*()}
function (\code{\link[=specify_chr]{specify_chr()}}, etc). This function is used to validate all
unnamed elements of \code{.x}. If \code{NULL} (default), any unnamed elements will
cause an error.}

\item{.allow_duplicate_names}{(\code{logical(1)}) Should \code{.x} be allowed to
have duplicate names? If \code{FALSE} (default), an error is thrown when any
named element of \code{.x} shares a name with another.}

\item{.unique}{(\code{logical(1)}) Should all elements in \code{.x} be distinct?
If \code{TRUE}, duplicated elements are rejected.}

\item{.allow_null}{(\code{logical(1)}) Is NULL an acceptable value?}

\item{.min_size}{(\code{integer(1)}) The minimum size of the object. Object
size will be tested using \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.}

\item{.max_size}{(\code{integer(1)}) The maximum size of the object. Object
size will be tested using \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.}

\item{.x_arg}{(\code{character(1)}) The name of the argument being stabilized
to use in error messages. The automatic value will work in most cases, or
pass it through from higher-level functions to make error messages clearer
in unexported functions.}

\item{.call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{.x_class}{(\code{character(1)}) The class name of the argument being
stabilized to use in error messages. Use this if you remove a special class
from the object before checking its coercion, but want the error message to
match the original class.}
}
\value{
The validated list, or an error condition with classes
\verb{<stbl-error>}, \verb{<stbl-condition>}, \verb{<rlang_error>}, \verb{<error>},
\verb{<condition>}, and a specific class by failure mode:
\itemize{
\item \verb{<stbl-error-bad_null>} for \code{NULL} values when \code{.allow_null = FALSE}.
\item \verb{<stbl-error-coerce-list>} when \code{.x} cannot be coerced to a list.
\item \verb{<stbl-error-size_too_small>} when the list is shorter than \code{.min_size}.
\item \verb{<stbl-error-size_too_large>} when the list is longer than \code{.max_size}.
\item \verb{<stbl-error-duplicate_elements>} when \code{.unique = TRUE} and duplicate
elements are present.
\item \verb{<stbl-error-unnamed_spec>} when any element passed through \code{...} is
unnamed.
\item \verb{<stbl-error-missing_element>} when a required named element is absent.
\item \verb{<stbl-error-bad_unnamed>} when unnamed elements are present but
\code{.unnamed} is \code{NULL}.
\item \verb{<stbl-error-bad_named>} when extra named elements are present but \code{.named}
is \code{NULL}.
\item \verb{<stbl-error-duplicate_names>} when duplicate names are present and
\code{.allow_duplicate_names = FALSE}.
}
}
\description{
\code{stabilize_lst()} validates the structure and contents of a list. It can
check that specific named elements are present and valid, that extra named
elements conform to a shared rule, and that unnamed elements conform to a
shared rule. \code{stabilise_lst()}, \code{stabilize_list()}, and \code{stabilise_list()}
are synonyms of \code{stabilize_lst()}.
}
\examples{
# Basic validation: named required elements
stabilize_lst(
  list(name = "Alice", age = 30L),
  name = specify_chr_scalar(),
  age = specify_int_scalar()
)

# Allow any non-NULL element with assert_present
stabilize_lst(list(data = mtcars), data = assert_present)

# Allow extra named elements via .named
stabilize_lst(
  list(a = 1L, b = 2L, c = 3L),
  .named = specify_int_scalar()
)

# Allow unnamed elements via .unnamed
stabilize_lst(list(1L, 2L, 3L), .unnamed = specify_int_scalar())

# NULL is allowed by default
stabilize_lst(NULL)
try(stabilize_lst(NULL, .allow_null = FALSE))

# Enforce size constraints
try(stabilize_lst(list(a = 1L), .min_size = 2))

# Reject duplicate names by default; opt in to allow them
try(stabilize_lst(list(a = 1L, a = 2L), .named = specify_int_scalar()))
stabilize_lst(
  list(a = 1L, a = 2L),
  .named = specify_int_scalar(),
  .allow_duplicate_names = TRUE
)
}
\seealso{
Other list functions:
\code{\link[=assert_present]{assert_present()}},
\code{\link[=specify_lst]{specify_lst()}},
\code{\link[=to]{to()}},
\code{\link[=to_lst]{to_lst()}}

Other stabilization functions:
\code{\link[=assert_present]{assert_present()}},
\code{\link[=stabilize_any_of]{stabilize_any_of()}},
\code{\link[=stabilize_arg]{stabilize_arg()}},
\code{\link[=stabilize_chr]{stabilize_chr()}},
\code{\link[=stabilize_chr_scalar]{stabilize_chr_scalar()}},
\code{\link[=stabilize_dbl]{stabilize_dbl()}},
\code{\link[=stabilize_dbl_scalar]{stabilize_dbl_scalar()}},
\code{\link[=stabilize_df]{stabilize_df()}},
\code{\link[=stabilize_fct]{stabilize_fct()}},
\code{\link[=stabilize_fct_scalar]{stabilize_fct_scalar()}},
\code{\link[=stabilize_int]{stabilize_int()}},
\code{\link[=stabilize_int_scalar]{stabilize_int_scalar()}},
\code{\link[=stabilize_lgl]{stabilize_lgl()}},
\code{\link[=stabilize_lgl_scalar]{stabilize_lgl_scalar()}},
\code{\link[=to_chr]{to_chr()}},
\code{\link[=to_chr_scalar]{to_chr_scalar()}},
\code{\link[=to_dbl]{to_dbl()}},
\code{\link[=to_dbl_scalar]{to_dbl_scalar()}},
\code{\link[=to_fct]{to_fct()}},
\code{\link[=to_fct_scalar]{to_fct_scalar()}},
\code{\link[=to_int]{to_int()}},
\code{\link[=to_int_scalar]{to_int_scalar()}},
\code{\link[=to_lgl]{to_lgl()}},
\code{\link[=to_lgl_scalar]{to_lgl_scalar()}}
}
\concept{list functions}
\concept{stabilization functions}
