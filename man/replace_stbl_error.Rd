% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_stbl_error.R
\name{replace_stbl_error}
\alias{replace_stbl_error}
\title{Replace a stbl error with a custom message}
\usage{
replace_stbl_error(
  expr,
  subclass,
  message,
  call = caller_env(),
  message_env = call,
  additional_class = character()
)
}
\arguments{
\item{expr}{An expression to evaluate.}

\item{subclass}{(\code{character}) The subclass(es) of the \code{{stbl}} error to
catch. Combined with \code{"stbl-error-"} to form the class name to intercept.
For example, \code{c("coerce", "character")} catches errors of class
\code{stbl-error-coerce-character}, while \code{c("coerce")} catches any
\code{stbl-error-coerce} error.}

\item{message}{(\code{character}) The replacement message. Formatted with
\code{\link[cli:cli_bullets]{cli::cli_bullets()}}.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{message_env}{(\code{environment}) The execution environment to use to
evaluate variables in error messages.}

\item{additional_class}{(\code{character}) Additional classes to prepend to the
error class list. These are used exactly as-is — no \code{"stbl-error-"} prefix
is added. Defaults to \code{character()} (no additional classes).}
}
\value{
The result of \code{expr}, or an error with the replacement message if
a matching \code{{stbl}} error is thrown.
}
\description{
Catches a \code{{stbl}} error with the specified \code{subclass} and replaces its
message with a new one. Use this to provide more context-specific error
messages when calling \code{{stbl}} functions inside your own package.
}
\details{
The \code{subclass} argument mirrors the subclass hierarchy used when the error
was originally thrown. For example, an error with class
\code{stbl-error-coerce-character} is caught with
\code{subclass = c("coerce", "character")}. Any \code{coerce} error (regardless of
the target type) is caught with \code{subclass = c("coerce")}. Similarly,
\code{stbl-error-incompatible_values-integer} is caught with
\code{subclass = c("incompatible_values", "integer")}.
}
\examples{
my_fn <- function(x) {
  replace_stbl_error(
    to_chr(x),
    subclass = c("coerce", "character"),
    message = "{.arg x} must be a character string."
  )
}
try(my_fn(data.frame()))

my_fn2 <- function(x) {
  replace_stbl_error(
    to_chr(x),
    subclass = c("coerce", "character"),
    message = "{.arg x} must be a character string.",
    additional_class = "mypkg-error-bad_chr"
  )
}
try(my_fn2(data.frame()))
}
