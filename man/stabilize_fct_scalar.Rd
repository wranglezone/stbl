% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabilize_fct.R
\name{stabilize_fct_scalar}
\alias{stabilize_fct_scalar}
\alias{stabilize_factor_scalar}
\alias{stabilise_fct_scalar}
\alias{stabilise_factor_scalar}
\title{Coerce to length-1 factor with additional checks}
\usage{
stabilize_fct_scalar(
  x,
  ...,
  allow_null = FALSE,
  allow_zero_length = FALSE,
  allow_na = TRUE,
  levels = NULL,
  to_na = character(),
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilize_factor_scalar(
  x,
  ...,
  allow_null = FALSE,
  allow_zero_length = FALSE,
  allow_na = TRUE,
  levels = NULL,
  to_na = character(),
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilise_fct_scalar(
  x,
  ...,
  allow_null = FALSE,
  allow_zero_length = FALSE,
  allow_na = TRUE,
  levels = NULL,
  to_na = character(),
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilise_factor_scalar(
  x,
  ...,
  allow_null = FALSE,
  allow_zero_length = FALSE,
  allow_na = TRUE,
  levels = NULL,
  to_na = character(),
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)
}
\arguments{
\item{x}{The object to stabilize.}

\item{...}{Arguments passed to methods.}

\item{allow_null}{(\code{logical(1)}) Is NULL an acceptable value?}

\item{allow_zero_length}{(\code{logical(1)}) Are zero-length vectors
acceptable?}

\item{allow_na}{(\code{logical(1)}) Are NA values ok?}

\item{levels}{\code{(character)} Expected levels. If \code{NULL} (default), the levels
will be computed by \code{\link[base:factor]{base::factor()}}.}

\item{to_na}{\code{(character)} Values to convert to \code{NA}.}

\item{x_arg}{(\code{character(1)}) The name of the object being stabilized
to use in error messages. The automatic value will work in most cases, or
pass it through from higher-level functions to make error messages clearer
in unexported functions.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{x_class}{(\code{character(1)}) The class name of the object being
stabilized to use in error messages. Use this if you remove a special class
from the object before checking its coercion, but want the error message to
match the original class.}
}
\value{
The input as a length-1 factor, or an error condition with classes
\verb{<stbl-error>}, \verb{<stbl-condition>}, \verb{<rlang_error>}, \verb{<error>},
\verb{<condition>}, and a specific class by failure mode:
\itemize{
\item \verb{<stbl-error-coerce-factor>} when \code{x} cannot be coerced to factor.
\item \verb{<stbl-error-bad_null>} for \code{NULL} values when \code{allow_null = FALSE}.
\item \verb{<stbl-error-bad_empty>} for empty vectors when
\code{allow_zero_length = FALSE}.
\item \verb{<stbl-error-non_scalar>} for non-scalar vectors.
\item \verb{<stbl-error-bad_na>} for \code{NA} values when \code{allow_na = FALSE}.
\item \verb{<stbl-error-fct_levels>} when values are not present in \code{levels}.
}
}
\description{
Checks whether a vector can be coerced to a length-1 factor.
\code{stabilize_fct_scalar()} is optimized to check for length-1 factors
(compared to \code{\link[=stabilize_fct]{stabilize_fct()}} with \code{max_size = 1}). \code{stabilise_fct_scalar},
\code{stabilize_factor_scalar()}, and \code{stabilise_factor_scalar} are synonyms of
\code{stabilize_fct_scalar()}.
}
\examples{
stabilize_fct_scalar("a")
try(stabilize_fct_scalar(letters))
try(stabilize_fct_scalar("c", levels = c("a", "b")))
}
\seealso{
Other factor functions:
\code{\link[=are_fct_ish]{are_fct_ish()}},
\code{\link[=specify_fct]{specify_fct()}},
\code{\link[=stabilize_fct]{stabilize_fct()}},
\code{\link[=to]{to()}},
\code{\link[=to_fct]{to_fct()}},
\code{\link[=to_fct_scalar]{to_fct_scalar()}}

Other stabilization functions:
\code{\link[=assert_present]{assert_present()}},
\code{\link[=stabilize_any_of]{stabilize_any_of()}},
\code{\link[=stabilize_arg]{stabilize_arg()}},
\code{\link[=stabilize_chr]{stabilize_chr()}},
\code{\link[=stabilize_chr_scalar]{stabilize_chr_scalar()}},
\code{\link[=stabilize_dbl]{stabilize_dbl()}},
\code{\link[=stabilize_dbl_scalar]{stabilize_dbl_scalar()}},
\code{\link[=stabilize_df]{stabilize_df()}},
\code{\link[=stabilize_fct]{stabilize_fct()}},
\code{\link[=stabilize_int]{stabilize_int()}},
\code{\link[=stabilize_int_scalar]{stabilize_int_scalar()}},
\code{\link[=stabilize_lgl]{stabilize_lgl()}},
\code{\link[=stabilize_lgl_scalar]{stabilize_lgl_scalar()}},
\code{\link[=stabilize_lst]{stabilize_lst()}},
\code{\link[=to_chr]{to_chr()}},
\code{\link[=to_chr_scalar]{to_chr_scalar()}},
\code{\link[=to_dbl]{to_dbl()}},
\code{\link[=to_dbl_scalar]{to_dbl_scalar()}},
\code{\link[=to_fct]{to_fct()}},
\code{\link[=to_fct_scalar]{to_fct_scalar()}},
\code{\link[=to_int]{to_int()}},
\code{\link[=to_int_scalar]{to_int_scalar()}},
\code{\link[=to_lgl]{to_lgl()}},
\code{\link[=to_lgl_scalar]{to_lgl_scalar()}}
}
\concept{factor functions}
\concept{stabilization functions}
