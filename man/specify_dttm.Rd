% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specify_cls.R
\name{specify_dttm}
\alias{specify_dttm}
\alias{specify_dttm_scalar}
\alias{specify_datetime}
\alias{specify_datetime_scalar}
\title{Create a specified datetime stabilizer function}
\usage{
specify_dttm(
  tz = "UTC",
  allow_null = TRUE,
  allow_na = TRUE,
  min_size = NULL,
  max_size = NULL,
  unique = FALSE,
  min_value = NULL,
  max_value = NULL,
  allowed_values = NULL
)

specify_dttm_scalar(
  tz = "UTC",
  allow_null = FALSE,
  allow_zero_length = FALSE,
  allow_na = TRUE,
  min_value = NULL,
  max_value = NULL,
  allowed_values = NULL
)

specify_datetime(
  tz = "UTC",
  allow_null = TRUE,
  allow_na = TRUE,
  min_size = NULL,
  max_size = NULL,
  unique = FALSE,
  min_value = NULL,
  max_value = NULL,
  allowed_values = NULL
)

specify_datetime_scalar(
  tz = "UTC",
  allow_null = FALSE,
  allow_zero_length = FALSE,
  allow_na = TRUE,
  min_value = NULL,
  max_value = NULL,
  allowed_values = NULL
)
}
\arguments{
\item{tz}{(\code{character(1)}) The time zone to normalize \code{x} to. Must be
\code{""} or a value from \code{\link[=OlsonNames]{OlsonNames()}}. Defaults to \code{"UTC"}.}

\item{allow_null}{(\code{logical(1)}) Is NULL an acceptable value?}

\item{allow_na}{(\code{logical(1)}) Are NA values ok?}

\item{min_size}{(\code{integer(1)}) The minimum size of the object. Object size
will be tested using \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.}

\item{max_size}{(\code{integer(1)}) The maximum size of the object. Object size
will be tested using \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.}

\item{unique}{(\code{logical(1)}) Should all elements in \code{x} be distinct?}

\item{min_value}{(\code{numeric(1)}) The lowest allowed value for \code{x}. If \code{NULL}
(default) values are not checked.}

\item{max_value}{(\code{numeric(1)}) The highest allowed value for \code{x}. If \code{NULL}
(default) values are not checked.}

\item{allowed_values}{A vector of permitted values (coerced to the target
type). \code{NULL} (default) skips the check. \code{NA} values in \code{x} are permitted
independently of \code{allowed_values}, subject to \code{allow_na}.}

\item{allow_zero_length}{(\code{logical(1)}) Are zero-length vectors acceptable?}
}
\value{
A function of class \code{"stbl_specified_fn"} that calls
\code{\link[=stabilize_dttm]{stabilize_dttm()}} or \code{\link[=stabilize_dttm_scalar]{stabilize_dttm_scalar()}} with the provided
arguments. The generated function will also accept \code{...} for additional
arguments to pass to \code{stabilize_dttm()} or \code{stabilize_dttm_scalar()}. You
can copy/paste the body of the resulting function if you want to provide
additional context or functionality.
}
\description{
\code{specify_dttm()} creates a function that will call \code{\link[=stabilize_dttm]{stabilize_dttm()}} with
the provided arguments. \code{specify_dttm_scalar()} creates a function that will
call \code{\link[=stabilize_dttm_scalar]{stabilize_dttm_scalar()}} with the provided arguments.
\code{specify_datetime()} is a synonym of \code{specify_dttm()}, and
\code{specify_datetime_scalar()} is a synonym of \code{specify_dttm_scalar()}.
}
\examples{
stabilize_recent <- specify_dttm(min_value = "2000-01-01T00:00:00Z")
stabilize_recent("2024-01-01T00:00:00Z")
try(stabilize_recent("1999-12-31T00:00:00Z"))
}
\seealso{
Other datetime functions:
\code{\link[=stabilize_dttm]{stabilize_dttm()}},
\code{\link[=stabilize_dttm_scalar]{stabilize_dttm_scalar()}},
\code{\link[=to_dttm]{to_dttm()}},
\code{\link[=to_dttm_scalar]{to_dttm_scalar()}}

Other specification functions:
\code{\link[=specify_chr]{specify_chr()}},
\code{\link[=specify_date]{specify_date()}},
\code{\link[=specify_dbl]{specify_dbl()}},
\code{\link[=specify_df]{specify_df()}},
\code{\link[=specify_dur]{specify_dur()}},
\code{\link[=specify_fct]{specify_fct()}},
\code{\link[=specify_int]{specify_int()}},
\code{\link[=specify_lgl]{specify_lgl()}},
\code{\link[=specify_lst]{specify_lst()}},
\code{\link[=specify_time]{specify_time()}}
}
\concept{datetime functions}
\concept{specification functions}
