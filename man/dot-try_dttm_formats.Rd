% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-datetime_formats.R
\name{.try_dttm_formats}
\alias{.try_dttm_formats}
\title{Try candidate date-time formats against a character vector}
\usage{
.try_dttm_formats(x, formats, tz)
}
\arguments{
\item{x}{\code{(character)} The vector to parse.}

\item{formats}{\code{(character)} Candidate \code{strptime()}-style format strings,
tried in order.}

\item{tz}{\code{(character(1))} The time zone to assume for date-times with no
explicit offset, and to attach to the result.}
}
\value{
A list with \code{parsed} (a \link[base:POSIXct]{base::POSIXct} vector, in \code{tz}) and
\code{failures} (a logical vector, the same length as \code{x}).
}
\description{
Like \code{\link[=.try_date_formats]{.try_date_formats()}}, but for date-time strings, with an optional
trailing UTC offset (\code{"Z"} or a numeric offset such as \code{"+05:00"}).
Elements with an offset are converted using that offset; elements without
one are treated as wall-clock time in \code{tz}.
}
\keyword{internal}
