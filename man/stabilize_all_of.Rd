% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabilize_all_of.R
\name{stabilize_all_of}
\alias{stabilize_all_of}
\alias{stabilise_all_of}
\title{Try to coerce or validate x as all of several specs}
\usage{
stabilize_all_of(
  x,
  ...,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilise_all_of(
  x,
  ...,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)
}
\arguments{
\item{x}{The object to stabilize.}

\item{...}{Unnamed stabilizer functions, such as \verb{stabilize_*} functions
(\code{\link[=stabilize_chr]{stabilize_chr()}}, etc.), \verb{to_*} functions (\code{\link[=to_chr]{to_chr()}}, etc.), or
functions produced by \verb{specify_*()} calls (\code{\link[=specify_chr]{specify_chr()}}, etc.). Each is
applied to the original \code{x}; \code{x} must pass every one of them, and they
must all return the same value.}

\item{x_arg}{(\code{character(1)}) The name of the object being stabilized to use
in error messages. The automatic value will work in most cases, or pass it
through from higher-level functions to make error messages clearer in
unexported functions.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{x_class}{(\code{character(1)}) The class name of the object being stabilized
to use in error messages. Use this if you remove a special class from the
object before checking its coercion, but want the error message to match
the original class.}
}
\value{
\code{x} coerced or validated by every function in \code{...}, or an error
condition with classes \verb{<stbl-error>}, \verb{<stbl-condition>},
\verb{<rlang_error>}, \verb{<error>}, \verb{<condition>}, and a specific class by
failure mode:
\itemize{
\item \verb{<stbl-error-empty_specs>} when no functions are supplied in \code{...}.
\item \verb{<stbl-error-named_spec>} when any element of \code{...} is named.
\item \verb{<stbl-error-cant_stabilize_all_of>} when any provided function fails.
\item \verb{<stbl-error-inconsistent_all_of>} when every function succeeds, but
they don't all produce the same coerced value.
}
}
\description{
\code{stabilize_all_of()} validates and coerces \code{x} by applying
every function in \code{...} to \code{x}, independently. \code{x} must satisfy every
spec, and all specs must agree on the coerced result; if any spec fails,
or specs disagree on the coerced value, an informative error is thrown.
\code{stabilise_all_of()} is a synonym.

Unlike \code{\link[=stabilize_any_of]{stabilize_any_of()}}, the order of \code{...} doesn't affect whether
\code{x} passes: every spec sees the same, original \code{x}, not the output of the
previous spec. Order only affects which spec's failure message is
reported first when several specs fail.
}
\examples{
# Returns x unchanged when all functions succeed
stabilize_all_of(1L, stabilize_int, stabilize_int_scalar)

# Each spec is applied to the original x, independently
stabilize_all_of("ab", specify_chr(regex = "^a"), specify_chr(regex = "b$"))

# Errors with a combined message when a spec fails
try(stabilize_all_of("a", stabilize_int, stabilize_chr))

# Errors because each spec sees the original "1" (a string); the second
# spec would succeed on 1L, but doesn't get the chance to see it
try(stabilize_all_of("1", stabilize_int, specify_dbl(coerce_character = FALSE)))

# Errors when specs succeed but disagree on the coerced value
# (stabilize_int() keeps 1L an integer; specify_dbl() makes it a double)
try(stabilize_all_of(1L, stabilize_int, specify_dbl()))
}
\seealso{
Other stabilization functions:
\code{\link[=assert_present]{assert_present()}},
\code{\link[=stabilize_any_of]{stabilize_any_of()}},
\code{\link[=stabilize_arg]{stabilize_arg()}},
\code{\link[=stabilize_chr]{stabilize_chr()}},
\code{\link[=stabilize_chr_scalar]{stabilize_chr_scalar()}},
\code{\link[=stabilize_date]{stabilize_date()}},
\code{\link[=stabilize_date_scalar]{stabilize_date_scalar()}},
\code{\link[=stabilize_dbl]{stabilize_dbl()}},
\code{\link[=stabilize_dbl_scalar]{stabilize_dbl_scalar()}},
\code{\link[=stabilize_df]{stabilize_df()}},
\code{\link[=stabilize_dttm]{stabilize_dttm()}},
\code{\link[=stabilize_dttm_scalar]{stabilize_dttm_scalar()}},
\code{\link[=stabilize_dur]{stabilize_dur()}},
\code{\link[=stabilize_dur_scalar]{stabilize_dur_scalar()}},
\code{\link[=stabilize_fct]{stabilize_fct()}},
\code{\link[=stabilize_fct_scalar]{stabilize_fct_scalar()}},
\code{\link[=stabilize_int]{stabilize_int()}},
\code{\link[=stabilize_int_scalar]{stabilize_int_scalar()}},
\code{\link[=stabilize_lgl]{stabilize_lgl()}},
\code{\link[=stabilize_lgl_scalar]{stabilize_lgl_scalar()}},
\code{\link[=stabilize_lst]{stabilize_lst()}},
\code{\link[=stabilize_time]{stabilize_time()}},
\code{\link[=stabilize_time_scalar]{stabilize_time_scalar()}},
\code{\link[=to_chr]{to_chr()}},
\code{\link[=to_chr_scalar]{to_chr_scalar()}},
\code{\link[=to_date]{to_date()}},
\code{\link[=to_date_scalar]{to_date_scalar()}},
\code{\link[=to_dbl]{to_dbl()}},
\code{\link[=to_dbl_scalar]{to_dbl_scalar()}},
\code{\link[=to_dttm]{to_dttm()}},
\code{\link[=to_dttm_scalar]{to_dttm_scalar()}},
\code{\link[=to_dur]{to_dur()}},
\code{\link[=to_dur_scalar]{to_dur_scalar()}},
\code{\link[=to_fct]{to_fct()}},
\code{\link[=to_fct_scalar]{to_fct_scalar()}},
\code{\link[=to_int]{to_int()}},
\code{\link[=to_int_scalar]{to_int_scalar()}},
\code{\link[=to_lgl]{to_lgl()}},
\code{\link[=to_lgl_scalar]{to_lgl_scalar()}},
\code{\link[=to_time]{to_time()}},
\code{\link[=to_time_scalar]{to_time_scalar()}}
}
\concept{stabilization functions}
