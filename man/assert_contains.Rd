% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert_contains.R
\name{assert_contains}
\alias{assert_contains}
\title{Require that x contains a number of elements matching a specification}
\usage{
assert_contains(
  x,
  spec,
  ...,
  min_matches = 1,
  max_matches = NULL,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)
}
\arguments{
\item{x}{The object to stabilize.}

\item{spec}{\verb{(function)} A single stabilizer or coercion function, such as
a \verb{to_*} function (\code{\link[=to_chr]{to_chr()}}, etc.), a \verb{stabilize_*} function
(\code{\link[=stabilize_chr]{stabilize_chr()}}, etc.), or a function produced by a \verb{specify_*()} call
(\code{\link[=specify_chr]{specify_chr()}}, etc.). Applied independently to each element of \code{x}.}

\item{...}{Arguments passed to methods.}

\item{min_matches}{(\code{integer(1)}) The minimum number of elements of \code{x}
that must match \code{spec}. Must be \verb{>= 1}.}

\item{max_matches}{(\code{integer(1)} or \code{NULL}) The maximum number of elements
of \code{x} that may match \code{spec}. Must be \verb{>= min_matches}. \code{NULL} (default)
skips the upper-bound check.}

\item{x_arg}{(\code{character(1)}) The name of the object being stabilized to use
in error messages. The automatic value will work in most cases, or pass it
through from higher-level functions to make error messages clearer in
unexported functions.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{x_class}{(\code{character(1)}) The class name of the object being stabilized
to use in error messages. Use this if you remove a special class from the
object before checking its coercion, but want the error message to match
the original class.}
}
\value{
\code{x}, unchanged, if the number of elements of \code{x} matching \code{spec}
is between \code{min_matches} and \code{max_matches}, or an error condition with
classes \verb{<stbl-error>}, \verb{<stbl-condition>}, \verb{<rlang_error>}, \verb{<error>},
\verb{<condition>}, and a specific class by failure mode:
\itemize{
\item \verb{<stbl-error-too_few_matches>} when fewer than \code{min_matches} elements
match \code{spec}.
\item \verb{<stbl-error-too_many_matches>} when more than \code{max_matches} elements
match \code{spec}.
}
}
\description{
\code{assert_contains()} applies \code{spec} to each element of \code{x} independently
and counts how many elements match. It returns \code{x} unchanged if that count
falls between \code{min_matches} and \code{max_matches} (inclusive), and errors
otherwise.
}
\examples{
# By default, at least 1 element must match spec
assert_contains(list("1", "a", "b"), stabilize_int)

# Require at least 2 matching elements
assert_contains(list("1", "2", "a"), stabilize_int, min_matches = 2)

# Errors because no elements are int-ish
try(assert_contains(list("a", "b"), stabilize_int))

# Errors because too many elements are int-ish
try(assert_contains(list("1", "2", "3"), stabilize_int, max_matches = 2))

# spec is applied to each element, not to x as a whole, so a scalar spec
# like stabilize_int_scalar() still matches every element of a list
assert_contains(
  list(1L, 2L, 3L),
  stabilize_int_scalar,
  min_matches = 2,
  max_matches = 3
)
}
\seealso{
Other stabilization functions:
\code{\link[=assert_not]{assert_not()}},
\code{\link[=assert_present]{assert_present()}},
\code{\link[=stabilize_all_of]{stabilize_all_of()}},
\code{\link[=stabilize_any_of]{stabilize_any_of()}},
\code{\link[=stabilize_arg]{stabilize_arg()}},
\code{\link[=stabilize_chr]{stabilize_chr()}},
\code{\link[=stabilize_chr_scalar]{stabilize_chr_scalar()}},
\code{\link[=stabilize_date]{stabilize_date()}},
\code{\link[=stabilize_date_scalar]{stabilize_date_scalar()}},
\code{\link[=stabilize_dbl]{stabilize_dbl()}},
\code{\link[=stabilize_dbl_scalar]{stabilize_dbl_scalar()}},
\code{\link[=stabilize_df]{stabilize_df()}},
\code{\link[=stabilize_dttm]{stabilize_dttm()}},
\code{\link[=stabilize_dttm_scalar]{stabilize_dttm_scalar()}},
\code{\link[=stabilize_dur]{stabilize_dur()}},
\code{\link[=stabilize_dur_scalar]{stabilize_dur_scalar()}},
\code{\link[=stabilize_fct]{stabilize_fct()}},
\code{\link[=stabilize_fct_scalar]{stabilize_fct_scalar()}},
\code{\link[=stabilize_int]{stabilize_int()}},
\code{\link[=stabilize_int_scalar]{stabilize_int_scalar()}},
\code{\link[=stabilize_lgl]{stabilize_lgl()}},
\code{\link[=stabilize_lgl_scalar]{stabilize_lgl_scalar()}},
\code{\link[=stabilize_lst]{stabilize_lst()}},
\code{\link[=stabilize_one_of]{stabilize_one_of()}},
\code{\link[=stabilize_time]{stabilize_time()}},
\code{\link[=stabilize_time_scalar]{stabilize_time_scalar()}},
\code{\link[=to_chr]{to_chr()}},
\code{\link[=to_chr_scalar]{to_chr_scalar()}},
\code{\link[=to_date]{to_date()}},
\code{\link[=to_date_scalar]{to_date_scalar()}},
\code{\link[=to_dbl]{to_dbl()}},
\code{\link[=to_dbl_scalar]{to_dbl_scalar()}},
\code{\link[=to_dttm]{to_dttm()}},
\code{\link[=to_dttm_scalar]{to_dttm_scalar()}},
\code{\link[=to_dur]{to_dur()}},
\code{\link[=to_dur_scalar]{to_dur_scalar()}},
\code{\link[=to_fct]{to_fct()}},
\code{\link[=to_fct_scalar]{to_fct_scalar()}},
\code{\link[=to_int]{to_int()}},
\code{\link[=to_int_scalar]{to_int_scalar()}},
\code{\link[=to_lgl]{to_lgl()}},
\code{\link[=to_lgl_scalar]{to_lgl_scalar()}},
\code{\link[=to_time]{to_time()}},
\code{\link[=to_time_scalar]{to_time_scalar()}}
}
\concept{stabilization functions}
