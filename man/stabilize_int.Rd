% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabilize_int.R
\name{stabilize_int}
\alias{stabilize_int}
\alias{stabilize_integer}
\alias{stabilise_int}
\alias{stabilise_integer}
\title{Coerce to integer with additional checks}
\usage{
stabilize_int(
  x,
  ...,
  allow_null = TRUE,
  allow_na = TRUE,
  coerce_character = TRUE,
  coerce_factor = TRUE,
  min_size = NULL,
  max_size = NULL,
  unique = FALSE,
  min_value = NULL,
  max_value = NULL,
  exclusive_min_value = NULL,
  exclusive_max_value = NULL,
  allowed_values = NULL,
  multiple_of = NULL,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilize_integer(
  x,
  ...,
  allow_null = TRUE,
  allow_na = TRUE,
  coerce_character = TRUE,
  coerce_factor = TRUE,
  min_size = NULL,
  max_size = NULL,
  unique = FALSE,
  min_value = NULL,
  max_value = NULL,
  exclusive_min_value = NULL,
  exclusive_max_value = NULL,
  allowed_values = NULL,
  multiple_of = NULL,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilise_int(
  x,
  ...,
  allow_null = TRUE,
  allow_na = TRUE,
  coerce_character = TRUE,
  coerce_factor = TRUE,
  min_size = NULL,
  max_size = NULL,
  unique = FALSE,
  min_value = NULL,
  max_value = NULL,
  exclusive_min_value = NULL,
  exclusive_max_value = NULL,
  allowed_values = NULL,
  multiple_of = NULL,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)

stabilise_integer(
  x,
  ...,
  allow_null = TRUE,
  allow_na = TRUE,
  coerce_character = TRUE,
  coerce_factor = TRUE,
  min_size = NULL,
  max_size = NULL,
  unique = FALSE,
  min_value = NULL,
  max_value = NULL,
  exclusive_min_value = NULL,
  exclusive_max_value = NULL,
  allowed_values = NULL,
  multiple_of = NULL,
  x_arg = caller_arg(x),
  call = caller_env(),
  x_class = object_type(x)
)
}
\arguments{
\item{x}{The object to stabilize.}

\item{...}{Arguments passed to methods.}

\item{allow_null}{(\code{logical(1)}) Is NULL an acceptable value?}

\item{allow_na}{(\code{logical(1)}) Are NA values ok?}

\item{coerce_character}{(\code{logical(1)}) Should character vectors such as "1"
and "2.0" be considered numeric-ish?}

\item{coerce_factor}{(\code{logical(1)}) Should factors with values such as "1"
and "2.0" be considered numeric-ish? Note that this package uses the
character value from the factor, while \code{\link[=as.integer]{as.integer()}} and \code{\link[=as.double]{as.double()}} use
the integer index of the factor.}

\item{min_size}{(\code{integer(1)}) The minimum size of the object. Object size
will be tested using \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.}

\item{max_size}{(\code{integer(1)}) The maximum size of the object. Object size
will be tested using \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.}

\item{unique}{(\code{logical(1)}) Should all elements in \code{x} be distinct?}

\item{min_value}{(\code{numeric(1)}) The lowest allowed value for \code{x}. If \code{NULL}
(default) values are not checked.}

\item{max_value}{(\code{numeric(1)}) The highest allowed value for \code{x}. If \code{NULL}
(default) values are not checked.}

\item{exclusive_min_value}{(\code{numeric(1)}) \code{x} must be strictly greater than
this value (JSON Schema \code{exclusiveMinimum} semantics). \code{NULL} (default)
skips the check.}

\item{exclusive_max_value}{(\code{numeric(1)}) \code{x} must be strictly less than
this value (JSON Schema \code{exclusiveMaximum} semantics). \code{NULL} (default)
skips the check.}

\item{allowed_values}{A vector of permitted values (coerced to the target
type). \code{NULL} (default) skips the check. \code{NA} values in \code{x} are permitted
independently of \code{allowed_values}, subject to \code{allow_na}.}

\item{multiple_of}{(\code{numeric(1)}, positive) \code{x} must be an integer multiple
of this value. \code{NULL} (default) skips the check. For doubles, a small
relative tolerance is applied to avoid floating-point false negatives.}

\item{x_arg}{(\code{character(1)}) The name of the object being stabilized to use
in error messages. The automatic value will work in most cases, or pass it
through from higher-level functions to make error messages clearer in
unexported functions.}

\item{call}{\code{(environment)} The execution environment to mention as the
source of error messages.}

\item{x_class}{(\code{character(1)}) The class name of the object being stabilized
to use in error messages. Use this if you remove a special class from the
object before checking its coercion, but want the error message to match
the original class.}
}
\value{
The input as an integer vector, or an error condition with classes
\verb{<stbl-error>}, \verb{<stbl-condition>}, \verb{<rlang_error>}, \verb{<error>},
\verb{<condition>}, and a specific class by failure mode:
\itemize{
\item \verb{<stbl-error-coerce-integer>} when \code{x} cannot be coerced to integer.
\item \verb{<stbl-error-incompatible_values-integer>} when some values cannot be
safely converted to integer.
\item \verb{<stbl-error-bad_null>} for \code{NULL} values when \code{allow_null = FALSE}.
\item \verb{<stbl-error-bad_na>} for \code{NA} values when \code{allow_na = FALSE}.
\item \verb{<stbl-error-size_too_small>} when the vector is shorter than \code{min_size}.
\item \verb{<stbl-error-size_too_large>} when the vector is longer than \code{max_size}.
\item \verb{<stbl-error-duplicate_elements>} when \code{unique = TRUE} and duplicates are
present.
\item \verb{<stbl-error-outside_range>} when values fall outside \code{min_value},
\code{max_value}, \code{exclusive_min_value}, or \code{exclusive_max_value}.
\item \verb{<stbl-error-allowed_values>} when values are not in \code{allowed_values}.
\item \verb{<stbl-error-not_multiple>} when values are not a multiple of
\code{multiple_of}.
}
}
\description{
Compared to \code{\link[=to_int]{to_int()}}, \code{stabilize_int()} checks more details, but is slower.
\code{stabilise_int()}, \code{stabilize_integer()}, and \code{stabilise_integer()} are
synonyms of \code{stabilize_int()}.
}
\examples{
stabilize_int(1:10)
stabilize_int("1")
stabilize_int(1 + 0i)
stabilize_int(NULL)
try(stabilize_int(NULL, allow_null = FALSE))
try(stabilize_int(c(1, NA), allow_na = FALSE))
try(stabilize_int(letters))
try(stabilize_int("1", coerce_character = FALSE))
try(stabilize_int(factor(c("1", "a"))))
try(stabilize_int(factor("1"), coerce_factor = FALSE))
try(stabilize_int(1:10, min_value = 3))
try(stabilize_int(1:10, max_value = 7))
try(stabilize_int(1:10, exclusive_min_value = 3))
try(stabilize_int(1:10, exclusive_max_value = 8))
try(stabilize_int(1:5, allowed_values = c(1L, 2L, 3L)))
try(stabilize_int(1:5, multiple_of = 2))
}
\seealso{
Other integer functions:
\code{\link[=are_int_ish]{are_int_ish()}},
\code{\link[=specify_int]{specify_int()}},
\code{\link[=stabilize_int_scalar]{stabilize_int_scalar()}},
\code{\link[=to]{to()}},
\code{\link[=to_int]{to_int()}},
\code{\link[=to_int_scalar]{to_int_scalar()}}

Other stabilization functions:
\code{\link[=assert_present]{assert_present()}},
\code{\link[=stabilize_any_of]{stabilize_any_of()}},
\code{\link[=stabilize_arg]{stabilize_arg()}},
\code{\link[=stabilize_chr]{stabilize_chr()}},
\code{\link[=stabilize_chr_scalar]{stabilize_chr_scalar()}},
\code{\link[=stabilize_date]{stabilize_date()}},
\code{\link[=stabilize_date_scalar]{stabilize_date_scalar()}},
\code{\link[=stabilize_dbl]{stabilize_dbl()}},
\code{\link[=stabilize_dbl_scalar]{stabilize_dbl_scalar()}},
\code{\link[=stabilize_df]{stabilize_df()}},
\code{\link[=stabilize_dttm]{stabilize_dttm()}},
\code{\link[=stabilize_dttm_scalar]{stabilize_dttm_scalar()}},
\code{\link[=stabilize_dur]{stabilize_dur()}},
\code{\link[=stabilize_dur_scalar]{stabilize_dur_scalar()}},
\code{\link[=stabilize_fct]{stabilize_fct()}},
\code{\link[=stabilize_fct_scalar]{stabilize_fct_scalar()}},
\code{\link[=stabilize_int_scalar]{stabilize_int_scalar()}},
\code{\link[=stabilize_lgl]{stabilize_lgl()}},
\code{\link[=stabilize_lgl_scalar]{stabilize_lgl_scalar()}},
\code{\link[=stabilize_lst]{stabilize_lst()}},
\code{\link[=stabilize_time]{stabilize_time()}},
\code{\link[=stabilize_time_scalar]{stabilize_time_scalar()}},
\code{\link[=to_chr]{to_chr()}},
\code{\link[=to_chr_scalar]{to_chr_scalar()}},
\code{\link[=to_date]{to_date()}},
\code{\link[=to_date_scalar]{to_date_scalar()}},
\code{\link[=to_dbl]{to_dbl()}},
\code{\link[=to_dbl_scalar]{to_dbl_scalar()}},
\code{\link[=to_dttm]{to_dttm()}},
\code{\link[=to_dttm_scalar]{to_dttm_scalar()}},
\code{\link[=to_dur]{to_dur()}},
\code{\link[=to_dur_scalar]{to_dur_scalar()}},
\code{\link[=to_fct]{to_fct()}},
\code{\link[=to_fct_scalar]{to_fct_scalar()}},
\code{\link[=to_int]{to_int()}},
\code{\link[=to_int_scalar]{to_int_scalar()}},
\code{\link[=to_lgl]{to_lgl()}},
\code{\link[=to_lgl_scalar]{to_lgl_scalar()}},
\code{\link[=to_time]{to_time()}},
\code{\link[=to_time_scalar]{to_time_scalar()}}
}
\concept{integer functions}
\concept{stabilization functions}
