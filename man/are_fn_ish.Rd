% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/are_fn_ish.R
\name{are_fn_ish}
\alias{are_fn_ish}
\alias{are_function_ish}
\alias{is_fn_ish}
\alias{is_function_ish}
\title{Check if an object can be safely coerced to a function}
\usage{
are_fn_ish(x, ...)

are_function_ish(x, ...)

is_fn_ish(x, ...)

is_function_ish(x, ...)
}
\arguments{
\item{x}{The object to check.}

\item{...}{Arguments passed to methods.}
}
\value{
\code{are_fn_ish()} returns a logical vector with the same length as the
input. \code{is_fn_ish()} returns a \verb{length-1 logical} (\code{TRUE} or \code{FALSE}).
}
\description{
\code{are_fn_ish()} is a vectorized predicate function that checks
whether each element of its input can be safely coerced to a function.
\code{are_function_ish()} is a synonym of \code{are_fn_ish()}.

\code{is_fn_ish()} is a scalar predicate that checks whether its input can be
safely coerced to a function by \code{\link[=to_fn]{to_fn()}}. \code{is_function_ish()} is a
synonym of \code{is_fn_ish()}.
}
\details{
\code{are_fn_ish()} returns \code{TRUE} for:
\itemize{
\item Functions (including lambda functions created with \code{~} or \verb{\\()})
\item Formulas (one-sided or two-sided, coercible via \code{\link[rlang:as_function]{rlang::as_function()}})
\item Character strings that are syntactically valid function names: either a
bare name (\code{"mean"}) or a namespaced name (\code{"pkg::fn"}). The check is
syntactic; it does not verify that the named function exists in any
environment.
}

\code{is_fn_ish()} returns \code{TRUE} for objects that \code{\link[=to_fn]{to_fn()}} can coerce without
error (assuming a matching function exists in the search path):
\itemize{
\item Functions (including lambda functions created with \code{~} or \verb{\\()})
\item One-sided or two-sided formulas
\item Single non-\code{NA} character strings (including namespaced \code{"pkg::fn"} names)
}

\code{NULL} and length-0 character vectors are \emph{not} considered function-ish
because they do not represent a callable object; \code{\link[=to_fn]{to_fn()}} converts them to
\code{NULL} only as a permissive special case controlled by \code{allow_null}.
}
\examples{
are_fn_ish(mean)
are_fn_ish(~ . + 1)
are_fn_ish(c("mean", "stats::median", NA, "", "1bad"))
are_fn_ish(NULL)
are_fn_ish(1L)

is_fn_ish(mean)
is_fn_ish("mean")
is_fn_ish("stats::median")
is_fn_ish(~ . + 1)
is_fn_ish(NULL)
is_fn_ish(1L)
}
\seealso{
Other function functions:
\code{\link[=to]{to()}},
\code{\link[=to_fn]{to_fn()}}

Other check functions:
\code{\link[=are_chr_ish]{are_chr_ish()}},
\code{\link[=are_dbl_ish]{are_dbl_ish()}},
\code{\link[=are_fct_ish]{are_fct_ish()}},
\code{\link[=are_int_ish]{are_int_ish()}},
\code{\link[=are_lgl_ish]{are_lgl_ish()}}
}
\concept{check functions}
\concept{function functions}
