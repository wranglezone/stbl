% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-conditions.R
\name{.expect_pkg_condition_snapshot}
\alias{.expect_pkg_condition_snapshot}
\title{Snapshot-test a package condition}
\usage{
.expect_pkg_condition_snapshot(
  obj_expr,
  package,
  class_components,
  expect_fn_name,
  expect_fn,
  check_installed_msg,
  transform,
  variant,
  env
)
}
\arguments{
\item{obj_expr}{An unevaluated expression (from \code{\link[rlang:enexpr]{rlang::enexpr()}}).}

\item{package}{\verb{(length-1 character)} The name of the package to use in
classes.}

\item{class_components}{(\code{list}) Passed as \code{...} to \code{expect_fn}.}

\item{expect_fn_name}{(\code{character(1)}) Name of the class-checking expectation
to look up or inject into \code{env}.}

\item{expect_fn}{(\code{function}) The function to inject if not already findable.}

\item{check_installed_msg}{(\code{character(1)}) The \code{"to ..."} string passed to
\code{\link[rlang:check_installed]{rlang::check_installed()}}.}

\item{transform}{(\code{function} or \code{NULL}) Optional function to scrub volatile
output (e.g. temp paths) before snapshot comparison. Passed through to
\code{\link[testthat:expect_snapshot]{testthat::expect_snapshot()}}.}

\item{variant}{(\code{character(1)} or \code{NULL}) Optional snapshot variant name.
Passed through to \code{\link[testthat:expect_snapshot]{testthat::expect_snapshot()}}.}

\item{env}{(\code{environment}) The environment in which \code{object} should be
evaluated. Assignments made inside \code{object} are visible to the caller after
this function returns. \code{\link[=expect_pkg_warning_classes]{expect_pkg_warning_classes()}} is temporarily
injected into \code{env} if it is not already findable, so this works even when
this package is not attached.}
}
\value{
The result of \code{\link[testthat:expect_snapshot]{testthat::expect_snapshot()}}, invisibly.
}
\description{
Snapshot-test a package condition
}
\keyword{internal}
