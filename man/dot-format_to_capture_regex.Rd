% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-datetime_formats.R
\name{.format_to_capture_regex}
\alias{.format_to_capture_regex}
\title{Convert a strptime()-style format string to a field-capturing regex}
\usage{
.format_to_capture_regex(fmt)
}
\arguments{
\item{fmt}{(\code{character(1)}) A \code{strptime()}-style format string built from
\verb{\%Y}, \verb{\%m}, \verb{\%d}, \verb{\%H}, \verb{\%M}, and \verb{\%S} specifiers and literal separators.}
}
\value{
A list with:
\itemize{
\item \code{regex}: the unanchored regex body (not yet wrapped in \code{^}/\code{$}).
\item \code{fields}: a character vector naming the calendar field (one of \code{"Y"},
\code{"m"}, \code{"d"}, \code{"H"}, \code{"M"}, or \code{"S"}) captured by each group, in the
order the groups appear in \code{regex}.
}
}
\description{
Unlike \code{\link[=.format_to_regex]{.format_to_regex()}}, this keeps track of which calendar field each
capture group corresponds to, so the captured digits can be reassembled
regardless of the order in which the format places them (for example
\code{"\%m/\%d/\%Y"} vs. \code{"\%Y-\%m-\%d"}).
}
\keyword{internal}
