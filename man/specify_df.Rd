% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specify_df.R
\name{specify_df}
\alias{specify_df}
\alias{specify_data_frame}
\title{Create a specified data frame stabilizer function}
\usage{
specify_df(
  ...,
  .extra_cols = NULL,
  .col_names = NULL,
  .min_rows = NULL,
  .max_rows = NULL,
  .allow_null = TRUE
)

specify_data_frame(
  ...,
  .extra_cols = NULL,
  .col_names = NULL,
  .min_rows = NULL,
  .max_rows = NULL,
  .allow_null = TRUE
)
}
\arguments{
\item{...}{Named \code{\link[=specify_chr]{specify_*()}} functions for required named
columns of \code{.x}. Each name corresponds to a required column in \code{.x}, and
the function is used to validate that column.}

\item{.extra_cols}{A single \code{\link[=specify_chr]{specify_*()}} function to validate
all columns of \code{.x} that are \emph{not} explicitly listed in \code{...}. If \code{NULL}
(default), any extra columns will cause an error.}

\item{.col_names}{\code{(character)} A character vector of column names that must
be present in \code{.x}. Any columns listed here that are absent from \code{.x} will
cause an error. Unlike \code{...}, this does not validate the column contents.}

\item{.min_rows}{\verb{(length-1 integer)} The minimum number of rows allowed in
\code{.x}. If \code{NULL} (default), the row count is not checked.}

\item{.max_rows}{\verb{(length-1 integer)} The maximum number of rows allowed in
\code{.x}. If \code{NULL} (default), the row count is not checked.}

\item{.allow_null}{\verb{(length-1 logical)} Is NULL an acceptable value?}
}
\value{
A function of class \code{"stbl_specified_fn"} that calls
\code{\link[=stabilize_df]{stabilize_df()}} with the provided arguments. The generated function will
also accept \code{...} for additional named column specifications to pass to
\code{\link[=stabilize_df]{stabilize_df()}}. You can copy/paste the body of the resulting function if
you want to provide additional context or functionality.
}
\description{
\code{specify_df()} creates a function that will call \code{\link[=stabilize_df]{stabilize_df()}} with the
provided arguments. \code{specify_data_frame()} is a synonym of \code{specify_df()}.
}
\examples{
stabilize_person_df <- specify_df(
  name = specify_chr_scalar(allow_na = FALSE),
  age = specify_int_scalar(allow_na = FALSE),
  .extra_cols = stabilize_present
)
stabilize_person_df(data.frame(name = "Alice", age = 30L, score = 99.5))
try(stabilize_person_df(data.frame(name = "Alice")))
}
\seealso{
Other data frame functions: 
\code{\link{stabilize_df}()}

Other specification functions: 
\code{\link{specify_chr}()},
\code{\link{specify_dbl}()},
\code{\link{specify_fct}()},
\code{\link{specify_int}()},
\code{\link{specify_lgl}()},
\code{\link{specify_lst}()}
}
\concept{data frame functions}
\concept{specification functions}
